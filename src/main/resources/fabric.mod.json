{
  "schemaVersion": 1,
  "id": "duperpatcher",
  "version": "${version}",
  "name": "DuperPatcher",
  "description": "Patches dragon egg duplication exploits using Mixins",
  "authors": ["TechnicFox"],
  "contact": {},
  "license": "All-Rights-Reserved",
  "environment": "*",
  "entrypoints": {
    "main": [
      "org.meeno.duperpatcher.Duperpatcher"
    ]
  },
  "mixins": [
    "duperpatcher.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric-api": "*",
    "minecraft": "${minecraft_version}"
  }
}
